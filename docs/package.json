{
  "name": "docs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm generate && vitepress dev",
    "build": "pnpm generate && vitepress build",
    "generate": "tsx ./.vitepress/scripts/generate.ts"
  }
}
